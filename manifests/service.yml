apiVersion: v1
kind: Service
metadata:
    name: leandrouekipipelinessampleappdev
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: leandrouekipipelinessampleappdev